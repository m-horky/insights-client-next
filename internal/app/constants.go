package app

var Version string = "development"

// ConfigPath points to a path on a filesystem where a configuration file is stored.
var ConfigPath string = "/etc/insights-client/insights-client.conf"

var LogPath string = "/var/log/insights-client/insights-client-next.log"
